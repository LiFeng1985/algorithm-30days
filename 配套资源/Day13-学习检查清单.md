# 📋 Day 13 - 学习检查清单

> **图的遍历详解**  
> **预计完成时间：90-120 分钟**

---

## ✅ 学习目标检查

### 知识理解
- [ ] 理解BFS（广度优先搜索）的原理
- [ ] 理解DFS（深度优先搜索）的原理
- [ ] 明白两种遍历的区别
- [ ] 掌握遍历的应用场景

### 核心概念
- [ ] 能解释"层序遍历"的含义
- [ ] 理解为什么要标记已访问
- [ ] 知道队列和栈在遍历中的作用
- [ ] 了解遍历的时间复杂度

### 代码能力
- [ ] 能手写BFS的实现
- [ ] 能手写DFS的递归和迭代实现
- [ ] 能用遍历解决实际问题
- [ ] 会分析遍历的复杂度

### 费曼输出
- [ ] 能向他人解释BFS和DFS
- [ ] 能用生活例子比喻遍历
- [ ] 能说清楚何时用哪种遍历

---

## 📚 知识点自查

### 必须理解的概念
```
□ BFS的基本思想（队列实现）
□ DFS的基本思想（递归/栈实现）
□  visited集合的作用
□ 两种遍历的区别
□ 时间复杂度分析
□ 实际应用
```

### 能够举例说明
```
□ 演示BFS的执行过程
□ 演示DFS的执行过程
□ 举出实际应用场景
```

### 代码能力检查
```
□ 能实现BFS
□ 能实现DFS（递归）
□ 能实现DFS（迭代）
□ 能测试和验证正确性
```

---

## 💡 自我测试题

### 快速问答（5 分钟）

**1. 什么是BFS？**（50 字以内）
```
你的答案：
_________________________________
_________________________________
```

**2. BFS和DFS有什么区别？**
```
你的答案：
_________________________________
_________________________________
```

**3. 为什么要标记已访问的节点？**
```
你的答案：
_________________________________
_________________________________
```

### 判断对错

- [ ] BFS使用栈实现
- [ ] DFS一定能找到最短路径
- [ ] BFS适合找最短路径
- [ ] 两种遍历的时间复杂度相同

**答案：**
1. ❌（BFS用队列，DFS用栈或递归）
2. ❌（DFS不保证最短路径）
3. ✅（BFS按层遍历，先到达的是最短的）
4. ✅（都是O(V+E)）

---

## 🎯 实践能力检查

### 代码小练习
实现以下图的遍历算法：

```javascript
// 1. BFS（广度优先搜索）
/**
 * @param {Graph} graph - 图对象
 * @param {*} startVertex - 起始顶点
 * @return {*[]} 遍历顺序
 */
function bfs(graph, startVertex) {
    const visited = ________________________;
    const queue = ________________________;
    const result = ________________________;
    
    visited.add(startVertex);
    queue.push(startVertex);
    
    while (_________________) {
        const vertex = ________________________;
        result.push(vertex);
        
        const neighbors = ________________________;
        for (const neighbor of neighbors) {
            const nextVertex = neighbor.vertex || neighbor;
            if (!_________________) {
                visited.add(nextVertex);
                ________________________;
            }
        }
    }
    
    return ______;
}

// 2. DFS 递归实现
/**
 * @param {Graph} graph - 图对象
 * @param {*} startVertex - 起始顶点
 * @return {*[]} 遍历顺序
 */
function dfsRecursive(graph, startVertex) {
    const visited = new Set();
    const result = [];
    
    function dfs(vertex) {
        visited.add(vertex);
        result.push(vertex);
        
        const neighbors = ________________________;
        for (const neighbor of neighbors) {
            const nextVertex = neighbor.vertex || neighbor;
            if (!_________________) {
                ________________________;
            }
        }
    }
    
    dfs(_________________);
    return ______;
}

// 3. DFS 迭代实现（使用栈）
/**
 * @param {Graph} graph - 图对象
 * @param {*} startVertex - 起始顶点
 * @return {*[]} 遍历顺序
 */
function dfsIterative(graph, startVertex) {
    const visited = ________________________;
    const stack = ________________________;
    const result = ________________________;
    
    visited.add(startVertex);
    stack.push(startVertex);
    
    while (_________________) {
        const vertex = ________________________;
        result.push(vertex);
        
        const neighbors = ________________________;
        // 逆序入栈，保证正序访问
        for (let i = neighbors.length - 1; i >= 0; i--) {
            const nextVertex = neighbors[i].vertex || neighbors[i];
            if (!_________________) {
                visited.add(nextVertex);
                ________________________;
            }
        }
    }
    
    return ______;
}

// 测试
const graph = new Graph(false);
graph.addEdge('A', 'B');
graph.addEdge('A', 'C');
graph.addEdge('B', 'D');
graph.addEdge('B', 'E');
graph.addEdge('C', 'F');

console.log('BFS:', bfs(graph, 'A'));
// 可能输出：['A', 'B', 'C', 'D', 'E', 'F']

console.log('DFS 递归:', dfsRecursive(graph, 'A'));
// 可能输出：['A', 'B', 'D', 'E', 'C', 'F']

console.log('DFS 迭代:', dfsIterative(graph, 'A'));
// 可能输出：['A', 'B', 'D', 'E', 'C', 'F']
```

### 生活观察家
找一找生活中类似遍历的例子：

```
BFS 的例子：_________________
说明：_________________

DFS 的例子：_________________
说明：_________________
```

### 小老师挑战
尝试向家人或朋友解释：

```
"你知道吗，BFS就像______
一层一层地探索；

而DFS就像______
一条路走到黑。"
```

如果能说清楚，说明你真懂了！✅

---

## ⏰ 学习时间分配

建议时间分配：
```
📖 阅读教程：35 分钟
💻 编写代码：45 分钟
✍️ 完成练习：20 分钟
🎤 费曼输出：15 分钟
📝 自我检查：10 分钟
```

实际用时：_____分钟

---

## 🌟 掌握程度自评

完成所有检查后，给自己的掌握程度打分：

```
完全不懂  ○○○○○  完全掌握
          1  2  3  4  5

我的评分：_____星

今天的收获：
_________________________________
_________________________________

还不太明白的地方：
_________________________________
_________________________________

明天想重点学习的内容：
_________________________________
_________________________________
```

---

## ✅ 完成认证

我已经完成了 Day 13 的所有学习内容！

签名：______________
日期：____年__月__日

家长/同伴签名：______________（可选）

---

## 🎁 奖励机制

完成今日学习后，给自己一个小奖励：
```
□ 吃喜欢的零食
□ 休息 15 分钟
□ 发朋友圈炫耀
□ 继续挑战下一课
□ 其他：_______________
```

---

## 🔗 与之前的联系

```
昨天学了：图结构基础
今天学了：图的遍历

联系：
□ 遍历是图的基本操作
□ BFS和DFS是后续算法的基础
□ 为最短路径算法打基础
```

---

**💪 第十三天就完成了，继续保持！**

**图的遍历非常重要，一定要掌握！**

**明天学习最短路径，加油！** 👋
